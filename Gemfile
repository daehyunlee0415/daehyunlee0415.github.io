# frozen_string_literal: true

source "https://rubygems.org"

# Pin to the same dependency set GitHub Pages uses in production.
# https://pages.github.com/versions/
gem "github-pages", group: :jekyll_plugins
